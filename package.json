{
  "name": "iris-regression-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Regression suite for the Iris Sciences Operational Console audit findings",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.11.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.4.0"
  }
}
